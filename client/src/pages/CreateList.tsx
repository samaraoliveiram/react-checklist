import React from "react";
import CreateList from "../components/CreateList";

const CreateListPage: React.FC<{ id: string }> = ({}) => {
  return (
    <>
      <CreateList />
    </>
  );
};

export default CreateListPage;
